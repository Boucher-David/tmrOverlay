# Status Mocks

Generated preview screenshots for the collector status overlay.

- `status-states.png` is the current mac-harness contact sheet.
- `states/waiting.png` shows the waiting-for-iRacing state.
- `states/live-analysis.png` shows live analysis with raw capture off.
- `states/raw-capture.png` shows healthy raw capture disk writes.
- `states/error.png` shows a visible failure state with frame/drop context.
