# Overlay Mocks

This folder stores static mock images for user feedback and design review.

Each overlay type should get its own subfolder:

```text
mocks/
  fuel-calculator/
  car-radar/
  standings/
  relative/
  competition-distance-graph/
  weather/
```

These files are design artifacts, not generated app output. Keep them small enough to review in git.
