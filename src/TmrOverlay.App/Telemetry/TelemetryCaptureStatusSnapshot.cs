namespace TmrOverlay.App.Telemetry;

internal sealed record TelemetryCaptureStatusSnapshot(
    bool IsConnected,
    bool IsCapturing,
    bool RawCaptureEnabled,
    bool RawCaptureActive,
    bool RawCaptureStopRequested,
    DateTimeOffset? RawCaptureStopRequestedAtUtc,
    DateTimeOffset? LastRawCaptureStoppedAtUtc,
    string? AppRunId,
    string? CurrentCollectionId,
    string? LastCollectionId,
    string? CurrentSourceId,
    string? LastSourceId,
    string? CurrentCaptureId,
    string? LastCaptureId,
    string? CaptureRoot,
    string? CurrentCaptureDirectory,
    string? LastCaptureDirectory,
    int FrameCount,
    int WrittenFrameCount,
    int DroppedFrameCount,
    long? TelemetryFileBytes,
    DateTimeOffset? CaptureStartedAtUtc,
    DateTimeOffset? LastFrameCapturedAtUtc,
    DateTimeOffset? LastDiskWriteAtUtc,
    int CaptureWriteStatusCount,
    long? LastCaptureWriteBytes,
    long? LastCaptureWriteElapsedMilliseconds,
    string? LastCaptureWriteKind,
    double? AverageCaptureWriteElapsedMilliseconds,
    long? MaxCaptureWriteElapsedMilliseconds,
    bool IsCaptureSynthesisPending,
    DateTimeOffset? CaptureSynthesisPendingSinceUtc,
    string? CaptureSynthesisPendingReason,
    bool IsCaptureSynthesisRunning,
    DateTimeOffset? CaptureSynthesisStartedAtUtc,
    int CaptureSynthesisSaveCount,
    DateTimeOffset? LastCaptureSynthesisSavedAtUtc,
    string? LastCaptureSynthesisPath,
    long? LastCaptureSynthesisBytes,
    long? LastCaptureSynthesisTelemetryBytes,
    long? LastCaptureSynthesisElapsedMilliseconds,
    long? LastCaptureSynthesisProcessCpuMilliseconds,
    double? LastCaptureSynthesisProcessCpuPercentOfOneCore,
    int? LastCaptureSynthesisTotalFrameRecords,
    int? LastCaptureSynthesisSampledFrameCount,
    int? LastCaptureSynthesisSampleStride,
    int? LastCaptureSynthesisFieldCount,
    bool IsHistoryFinalizing,
    DateTimeOffset? HistoryFinalizationStartedAtUtc,
    int HistorySummarySaveCount,
    DateTimeOffset? LastHistorySavedAtUtc,
    string? LastHistorySummaryLabel,
    long? LastHistoryFinalizationElapsedMilliseconds,
    long? LastHistorySaveElapsedMilliseconds,
    long? LastAnalysisSaveElapsedMilliseconds,
    string? AppWarning,
    string? LastWarning,
    string? LastError,
    DateTimeOffset? LastIssueAtUtc);
