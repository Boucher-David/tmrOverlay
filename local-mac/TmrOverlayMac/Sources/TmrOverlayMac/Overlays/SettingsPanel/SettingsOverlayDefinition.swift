import AppKit

enum SettingsOverlayDefinition {
    static let definition = OverlayDefinition(
        id: "settings",
        displayName: "Settings",
        defaultSize: NSSize(width: 1152, height: 608)
    )
}
